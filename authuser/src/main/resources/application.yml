server:
  port: 8087
  servlet:
    context-path: '/ead-authuser/'

spring:
  application:
    name: ead-authuser-service
  datasource:
    url: jdbc:postgresql://172.20.217.157:5432/ead-authuser-v2
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      dialect:
      jdbc:
        lob.non-contextual-creation: true
    properties:
      hibernate:
        show_sql: true
  output:
    ansi:
      enabled: always
  rabbitmq:
    addresses: amqps://fqvxjwyl:PDXrZmgdsAw09m_XTJ9iTgEVwFS_tiLT@moose.rmq.cloudamqp.com/fqvxjwyl

logging:
  level:
    com.ead: TRACE #apenas para o pacote
    root: INFO  #log de toda aplicação, pasta root
    org.springframework.web: DEBUG #mostra os detalhes da requisição
    org.hibernate: INFO
ead:
  api:
    url:
      course: 'http://ead-course-service/ead-course'

eureka:
  client:
    serviceUrl:
      defaultZone: 'http://localhost:8761/eureka'
  instance:
    hostname: localhost


